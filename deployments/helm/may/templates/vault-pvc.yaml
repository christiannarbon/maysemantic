{{- if .Values.vault.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-vault-data
  labels:
    app: {{ .Release.Name }}-vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.vault.storage }}
{{- end }}
