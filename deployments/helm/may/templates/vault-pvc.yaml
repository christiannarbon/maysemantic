{{- if .Values.vault.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: may-vault-data
  labels:
    app: may-vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.vault.storage }}
{{- end }}
