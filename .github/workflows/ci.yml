name: CI

on:
  push:
    branches:
      - main
      - "feature/*"
      - "hotfix/*"
      - "release/*"
  pull_request:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always
  NODE_OPTIONS: "--no-deprecation"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

jobs:
  build:
    name: Format, Lint, and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2.9.1

      - name: Check Formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run Unit Tests
        run: cargo test --workspace --test unit

      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: pgwire/Dockerfile

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image (Verify Only)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: pgwire/Dockerfile
          push: false
          load: true
          tags: may_pgwire:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Start PGWire Service Container
        run: |
          docker run -d --name pgwire-test -p 5432:5432 \
            -v ${{ github.workspace }}/demos/valid_demo:/app:ro \
            may_pgwire:test

      - name: Verify Query Execution & Model Loading
        run: |
          # Install pg_isready/psql on ubuntu runner
          sudo apt-get update && sudo apt-get install -y postgresql-client
          
          # Wait up to 10 seconds for the service to accept connections
          for i in {1..10}; do
            if pg_isready -h 127.0.0.1 -p 5432; then
              echo "PGWire container is ready!"
              break
            fi
            echo "Waiting for container boot..."
            sleep 1
          done
          
          # Query the container and check the mock tabular response
          RESULT=$(PGPASSWORD=dummy psql -h 127.0.0.1 -p 5432 -U postgres -d postgres -c "SELECT * FROM users;" -t | tr -d '[:space:]')
          echo "Query Result: $RESULT"
          
          if [[ "$RESULT" == *"Alice"* ]] && [[ "$RESULT" == *"Bob"* ]]; then
            echo "Mock response verified!"
          else
            echo "Verification failed: expected mock tabular rows containing Alice and Bob"
            docker logs pgwire-test
            exit 1
          fi
          
          # Verify that the semantic model was loaded successfully from demos/valid_demo
          if docker logs pgwire-test 2>&1 | grep -q "Successfully loaded 1 models"; then
            echo "Model loading verified!"
          else
            echo "Verification failed: expected 1 loaded model in logs"
            docker logs pgwire-test
            exit 1
          fi

      - name: Clean up test container
        if: always()
        run: docker rm -f pgwire-test || true

  integration-test:
    name: Pagila Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2.9.1

      - name: Start Pagila Database
        run: docker compose up -d --wait
        working-directory: tests/integration/env

      - name: Run Pagila Integration Tests
        run: cargo test --workspace --test integration
        env:
          PAGILA_TESTS: "1"

      - name: Stop Pagila Database
        if: always()
        run: docker compose down -v
        working-directory: tests/integration/env
